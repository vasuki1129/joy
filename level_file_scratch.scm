;;this is the default scene
;;the engine starts up in this state
;;and many of the convenience functions
;;operate on the root object
;;it should not be changed, other than the scripts and
;;children fields


'(
     "root" ;name
     (0 0) ;position
     0.0 ;rotation
     (1 1) ;scale
     () ;children
     () ;init script
     () ;process script
     () ;render script
)
